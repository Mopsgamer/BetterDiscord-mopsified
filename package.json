{
	"name": "betterdiscord-rewrite",
	"private": true,
	"workspaces": {
		"packages": [
			"packages/*",
			"extension",
			"installer"
		],
		"catalog": {
			"react": "19.x",
			"electron": "latest"
		}
	},
	"type": "module",
	"scripts": {
		"cli": "bun --bun ./packages/cli/dist/main.js",
		"get": "bun ./packages/injection/scripts/get.ts",
		"ui": "bun -e \"const fs=require('fs'), path=require('path'); const dist=path.join('installer', 'dist'); const exe=fs.readdirSync(dist).find(f => f.startsWith('BetterDiscord') && (f.endsWith('.exe') || !f.includes('.'))); if (exe) require('child_process').spawn(path.join(dist, exe), {detached:true, stdio:'ignore'}).unref(); else console.error('Binary not found in installer/dist')\"",
		"lint": "bun oxlint --fix",
		"fmt": "bun oxfmt",
		"dist": "bun tsgo -p . && bun -F '*' dist",
		"dts": "bun tsgo -p . && bun -F '*' dts"
	},
	"dependencies": {
		"original-fs": "latest"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "latest",
		"@typescript/native-preview": "latest",
		"electron": "latest",
		"oxfmt": "latest",
		"oxlint": "latest",
		"publint": "latest"
	},
	"engines": {
		"bun": ">=1.3.13"
	}
}

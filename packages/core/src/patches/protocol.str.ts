export default "var _electron = require(\"electron\");\nvar _nodeFs = require(\"node:fs\");\nvar _index = \"Object.defineProperty(exports, \\\"__esModule\\\", {\\n  value: true\\n});\\nexports.Patcher = void 0;\\nconsole.log(\\\"BetterDiscord Core Loaded.\\\");\\nclass Patcher {}\\nexports.Patcher = Patcher;\";\nvar _nodePath = require(\"node:path\");\n// called after app is ready\nconst bdPath = (0, _nodePath.join)(_electron.app.getPath(\"userData\"), \"BetterDiscord\");\nconst extensionPath = (0, _nodePath.join)(bdPath, \"extension\");\n_electron.protocol.handle(\"bd\", async request => {\n  const url = new URL(request.url);\n  const pathName = url.pathname.replace(/^\\\\\\/+/, \"\");\n  if (pathName === \"api\") {\n    return new Response(_index.default, {\n      headers: {\n        \"Content-Type\": \"application/javascript\"\n      }\n    });\n  }\n  if (pathName === \"plugins\" || pathName === \"themes\") {\n    const dir = (0, _nodePath.join)(bdPath, pathName);\n    let ids = [];\n    if ((0, _nodeFs.existsSync)(dir)) {\n      const ext = {\n        plugins: \"js\",\n        themes: \"css\"\n      };\n      ids = (0, _nodeFs.readdirSync)(dir).filter(addonPath => (0, _nodeFs.existsSync)((0, _nodePath.join)(dir, addonPath, \"index.\" + ext[pathName])));\n    }\n    return new Response(`export default ${JSON.stringify(ids)};`, {\n      headers: {\n        \"Content-Type\": \"application/javascript\"\n      }\n    });\n  }\n  if (pathName.startsWith(\"import/\")) {\n    const relativePath = pathName.replace(\"import/\", \"\");\n    const fullPath = (0, _nodePath.join)(bdPath, relativePath);\n    if ((0, _nodeFs.existsSync)(fullPath)) {\n      return new Response((0, _nodeFs.readFileSync)(fullPath), {\n        headers: {\n          \"Content-Type\": \"application/javascript\"\n        }\n      });\n    }\n  }\n  return new Response(\"Not Found\", {\n    status: 404\n  });\n});\nif ((0, _nodeFs.existsSync)(extensionPath)) {\n  await _electron.session.defaultSession.extensions.loadExtension(extensionPath, {\n    allowFileAccess: true\n  });\n}" as string;
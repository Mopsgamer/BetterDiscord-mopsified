{
    "name": "@betterdiscord.com/injection",
    "version": "1.0.0",
    "private": false,
    "type": "module",
    "main": "./dist/index.js",
    "scripts": {
        "lint": "bun oxlint --fix",
        "fmt": "bun oxfmt",
        "dist": "bun dts && bun build --target node --production src/index.ts --outfile=./dist/index.js -e '@electron/asar'",
        "dts": "bun tsgo --declaration --emitDeclarationOnly --outDir ./dist"
    },
    "dependencies": {
        "@electron/asar": "^3.2.0"
    }
}